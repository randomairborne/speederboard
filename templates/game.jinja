{% extends "base.jinja" %}
{% block extraheaders %}{% endblock extraheaders %}
{% block title %}game{% endblock title %}
{% block body %}
<table>
    <tr>
        <th>user</th>
        <th>time</th>
        <th>score</th>
        <th>link</th>
    </tr>
    {% for run in runs %}
    <tr>
        <td><a href="{{ root_url }}/user/{{ run.submitter.username }}">{{ run.submitter.username }}</a></td>
        <td>{{ run.time }}</td>
        <td>{{ run.score }}</td>
        <td><a href="{{ run.video }}">{{ run.video }}</a></td>
    </tr>
    {% endfor %}
</table>
{{ __tera_context }}
{% endblock body %}